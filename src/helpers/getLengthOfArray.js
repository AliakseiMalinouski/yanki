export const getLengthOfArray = (firstArray, secondArray) => {
    if(firstArray && secondArray) {
        return {
            firstLength: firstArray.length,
            secondLength: secondArray.length,
        }
    }
}